{% extends 'layouts/master.twig' %}

{% block content %}
  <div class="container">
      <div class="col-sm-12">

          <h1>Home</h1>

          <p>Run `npm install`, or `yarn` to install dependencies from `package.json`</p>

          <p>Run `composer install` for PHP dependencies</p>

          <p>Check out the scripts in `package.json` for different commands</p>

      </div>
  </div>
{% endblock %}
