{% extends 'layouts/master.twig' %}

{% block content %}
  <div class="container">
      <div class="col-sm-12">

          <h1>Home</h1>

          <p>Run <code>npm install</code>, or <code>yarn</code> to install dependencies from <code>package.json</code></p>

          <p>Run <code>composer install</code> for PHP dependencies</p>

          <p>Check out the scripts in <code>package.json</code> for different commands</p>

      </div>
  </div>
{% endblock %}
